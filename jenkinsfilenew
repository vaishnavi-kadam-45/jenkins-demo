pipeline{
  agent any
  stages{
    stage("Install apache"){
      steps{
        sh 'sudo apt install apache -y'
        sh 'sudo service apache start'
      }
    }
    stage("Deployment"){
    steps{
     sh 'sudo cp * /var/www/html/'
    }
    }
  }
}
